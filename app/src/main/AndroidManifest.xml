<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sciprojekti">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Profiilinluonti"
                  android:parentActivityName=".ProfiiliActivity" />

        <activity android:name=".ProfiiliActivity"
                  android:parentActivityName=".MainActivity" />

        <activity android:name=".JaaKauppalistaActivity"
                  android:parentActivityName=".MainActivity" />

        <activity android:name=".KauppalistaLahetetty"
                  android:parentActivityName=".MainActivity" />

        <activity android:name=".KauppalistaActivity" />

        <activity android:name=".ViivakoodiActivity" />

        <activity android:name=".CountryListActivity"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ModifyCountryActivity"
            android:theme="@style/Theme.Transparent" />

        <activity
            android:name=".AddCountryActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.Dialog" />
        <activity android:name=".viivakoodiTulokset"></activity>
    </application>

</manifest>